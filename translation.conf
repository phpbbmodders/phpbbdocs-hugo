# Configuration for translations.sh.
#
# Path to the phpbbdocs-languages sibling checkout, resolved relative
# to this file's directory (the phpbbdocs-hugo repo root). Override
# with the LANGUAGES_REPO environment variable for CI or other
# checkout layouts, e.g.:
#   LANGUAGES_REPO=/tmp/phpbbdocs-languages ./translations.sh build fr
LANGUAGES_REPO=../phpbbdocs-languages
